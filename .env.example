# Copy to .env and set your values

# Host directory to mount as read-only /pages inside the container.
# Puppeteer can then access file:///pages/... paths.
PAGES_DIR=/path/to/your/project

# Port smth listens on (default: 3000)
# SMTH_PORT=3000
