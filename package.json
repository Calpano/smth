{
  "name": "smth",
  "version": "1.0.0",
  "description": "smth MCP tool — as reliable as Agent Smith",
  "type": "module",
  "main": "src/server.js",
  "bin": {
    "smth": "bin/smth.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "docker compose --profile test run --rm test",
    "test:build": "docker compose --profile test build test",
    "test:cli": "./run-cli-tests.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "express": "^5.0.0",
    "puppeteer-core": "^24.0.0"
  }
}
