import { describe, it, expect } from 'vitest';

describe('ItemStore', () => {
  it('adds an item', () => {
    expect(true).toBe(true);
  });

  it('removes an item', () => {
    expect(true).toBe(true);
  });
});
